# Dev_20260613_SAC_v0.2.0_Sprint01_Codex_Reverification_Report_v1

## 1. Verdict

**PASS_WITH_ADVISORY**

The prior Codex `REWORK_REQUIRED` issue is fixed. The relay capability token target-binding gap no longer reproduces, and the new regression tests cover missing, wrong, and matching document/path targets.

Sprint 02 may proceed from the Sprint 01 relay-foundation perspective, with the existing full-suite failures tracked separately.

## 2. Verified Inputs

- Rework completion report: `docs/reports/Rework_Report_Sprint_v020_Sprint01.md`
- Prior verification report: `docs/reports/Dev_20260613_SAC_v0.2.0_Sprint01_Codex_Verification_Report_v1.md`
- Repo: `C:\Projects\svil-archive-core`
- Branch: `master`
- HEAD: `a9e0ee1 fix: refresh Windows autostart cmd when build path changes`
- Worktree: dirty before and after verification; Sprint 01 and rework files remain uncommitted.

## 3. Rework Verification

### Fixed Area

File: `app/flutter_app/lib/data/relay/relay_capability_token_service.dart`

Confirmed behavior:

- Stored `target_document_id` now requires a non-empty result `targetDocumentId` and exact match.
- Stored `target_path` now requires a non-empty result `targetPath` and normalized path match.
- Non-empty `allowed_target` must match either the result document id or normalized result path.
- Path comparison uses `normalizePlatformPath()`, slash normalization, and lowercase comparison.

The previous bypass case, "valid token + matching action + missing target", is now rejected.

### Regression Coverage

File: `app/flutter_app/test/sprint_v020_sprint01_integration_test.dart`

New/expanded tests confirmed:

- scoped token + missing `target_document_id` => rejected and review inbox path
- wrong `target_document_id` => rejected
- matching `target_document_id` => accepted
- missing / wrong / matching `target_path` => rejected / rejected / accepted

## 4. Test Results

Executed from `C:\Projects\svil-archive-core\app\flutter_app`.

- `flutter analyze`: PASS, no issues found
- `flutter test test/sprint_v020_sprint01_integration_test.dart`: PASS, 15/15
- `flutter test test/sprint16_integration_test.dart test/sprint16h_integration_test.dart`: PASS, 34/34
- `flutter test`: FAIL, 264 PASS / 12 FAIL

The full-suite failures remain outside the reworked capability-token path. Visible failure groups are still report consistency / RC approval expectations and Windows temp-directory lock cleanup, including:

- `sprint12b_integration_test`: report consistency mismatch and temp delete lock
- `sprint11_integration_test`: RC finalization status expected warning/ready but got blocked
- `sprint12_integration_test`: release approval status expected waiting/ready/draft but got blocked
- `sprint9_integration_test`: report consistency and integrity summary expectations

## 5. Advisory

The rework is acceptable for the Sprint 01 blocker. Commit/push is reasonable if the intended scope includes only the Sprint 01 implementation, rework, and verification/report files.

Before committing, inspect staging carefully because the worktree contains many unrelated modified/deleted/untracked files from earlier work. Do not stage unrelated docs, assets, bins, or existing dirty files unless explicitly intended.

## 6. Sprint 02 Readiness

**Ready with advisory.**

From the relay foundation and capability-token safety boundary, Sprint 01 can move forward. The remaining full-suite failures should be tracked separately and should not be hidden as green.
